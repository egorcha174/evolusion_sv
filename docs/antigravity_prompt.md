# 🎯 ПРОМТ ДЛЯ ANTIGRAVITY: Компонент CameraCard для Home Assistant Lovelace

---

## КОНТЕКСТ И ЦЕЛЬ

Я работаю над SvelteKit фронтом для Home Assistant Lovelace. Нужно создать **профессиональный, надёжный компонент** для вывода видеопотоков от камер через go2rtc.

**Предыдущие попытки сломались** из-за:
- Проблем с SSR/hydration в SvelteKit
- Неправильной работы `srcObject` на `<video>` элементах
- Утечек памяти (незакрытые WebSocket, незаписанные потоки)
- Неправильного управления жизненным циклом компонентов

Я провёл углубленное исследование и составил **полное ТЗ с примерами кода и гарантиями что это работает**.

---

## ТВОЯ ЗАДАЧА

Реализовать **ровно то, что описано ниже**. **Без фантазии, без "улучшений", без отклонений.**

Если что-то кажется странным — это намеренно и объяснено в ТЗ. Всё проверено и протестировано.

---

## 📋 ТРЕБОВАНИЯ К АРХИТЕКТУРЕ

### Файлы которые нужно создать (в этом порядке)

1. **src/lib/types/camera.ts** — TypeScript интерфейсы
2. **src/lib/components/CameraCard.svelte** — главный компонент (~100 строк)
3. **src/lib/components/CameraCardPreview.svelte** — режим preview/MJPEG (~60 строк)
4. **src/lib/components/CameraCardLive.svelte** — режим live/WebRTC (~150 строк)

### Архитектурные ограничения (СТРОГИЕ)

- ✅ Всё внутри `onMount` для работы с видео (window, navigator, WebRTC API)
- ✅ Все потоки и таймеры очищаются в `onDestroy` или return функции из `onMount`
- ✅ **Использовать Svelte Action** для привязки `srcObject` к `<video>` элементу
- ✅ Каждый компонент имеет console.log с префиксом `[ComponentName]`
- ✅ Все fetch/WebSocket операции обёрнуты в try-catch
- ✅ Компоненты работают с SSR (не падают при сборке)

---

## 📋 КРИТИЧЕСКИЕ НЮАНСЫ

### Нюанс #1: SSR и onMount

**Правило**: Весь код, обращающийся к `window`, `navigator`, `WebRTC API` — **ТОЛЬКО внутри `onMount`**.

**Почему**: SvelteKit сначала рендеришь на сервере (нет `window`), потом на клиенте. Если код выполнится на сервере — упадёт.

```svelte
// ❌ НЕПРАВИЛЬНО
$effect(() => {
  let host = window.location.href; // Выполнится на сервере → ошибка
});

// ✅ ПРАВИЛЬНО
onMount(() => {
  let host = window.location.href; // Только на клиенте
});
```

### Нюанс #2: srcObject и Svelte Action

**Правило**: Не используй `bind:srcObject` (его нет). Используй Svelte Action.

**Почему**: Svelte не умеет биндить JS свойства, только HTML атрибуты. Прямое присвоение требует action.

```svelte
// ❌ НЕПРАВИЛЬНО
<video bind:srcObject={stream}></video>
<!-- Попытка установить атрибут srcobject="[object MediaStream]" → чёрный экран -->

// ✅ ПРАВИЛЬНО
<script>
  function srcObjectAction(node, stream) {
    node.srcObject = stream; // Прямое присвоение JS свойству
    return {
      update(newStream) { if (node.srcObject !== newStream) node.srcObject = newStream; },
      destroy() { node.srcObject = null; }
    };
  }
</script>

<video use:srcObjectAction={stream}></video>
```

### Нюанс #3: Очистка ресурсов

**Правило**: Каждый `setInterval`, `WebSocket`, `RTCPeerConnection`, `MediaStream` — должны быть закрыты при демонтировании.

**Почему**: Компоненты на Lovelace часто размонтируются. Незакрытые ресурсы = утечки памяти, браузер падает.

```svelte
onMount(() => {
  let interval = setInterval(() => { ... }, 1000);
  let ws = new WebSocket(url);

  return () => {
    clearInterval(interval);
    if (ws) ws.close();
    mediaStream?.getTracks().forEach(t => t.stop());
  };
});
```

---

## 📋 ТИПЫ (src/lib/types/camera.ts)

Используй эти типы:

```typescript
export interface CameraConfig {
  name: string;
  source: string;
  mode: 'preview' | 'live';
  go2rtcHost: string;
  go2rtcProtocol?: 'http' | 'https';
  previewRefreshInterval?: number;
  previewObjectFit?: 'cover' | 'contain';
  webrtcReconnectInterval?: number;
  webrtcTimeout?: number;
  webrtcMaxReconnectAttempts?: number;
  showTitle?: boolean;
  showLoadingSpinner?: boolean;
  aspectRatio?: string;
  onError?: (error: Error) => void;
  onReady?: () => void;
}

export interface CameraCardState {
  isLoading: boolean;
  isConnected: boolean;
  error: string | null;
}
```

---

## 📋 CameraCard.svelte — Главный компонент

### Props

```svelte
export let config: CameraConfig;       // Конфиг камеры
export let forceMode: 'preview' | 'live' | null = null;
export let isVisible: boolean = true;  // Для lazy loading
```

### Состояние

- `isLoading` — true во время подключения
- `isConnected` — true если видео идёт
- `error` — сообщение об ошибке (если есть)
- `currentMode` — текущий режим (может быть перегрдеп forceMode)

### Логика

```
onMount:
  ├─ Проверить isVisible
  ├─ Установить isLoading = true
  └─ Лог: [CameraCard] Mounting camera "{name}" in mode: {mode}

Рендер:
  ├─ Если !isVisible → показать placeholder
  ├─ Если isLoading → показать спиннер + "Connecting..."
  ├─ Если error → показать ошибку + кнопка Retry
  ├─ Если mode === 'preview' → <CameraCardPreview />
  └─ Если mode === 'live' → <CameraCardLive />

onDestroy:
  └─ Лог: [CameraCard] Unmounting camera "{name}"
```

### Обработчики событий

- `handleError(error)` — приходит из дочерних компонентов, показывает ошибку
- `handleReady()` — приходит из дочерних компонентов, устанавливает isConnected = true

---

## 📋 CameraCardPreview.svelte — MJPEG режим

### Задачи

- Выводить MJPEG поток как `<img src="http://host:1984/api/stream.mjpeg?src=name&t=now" />`
- Обновлять URL каждые N миллисекунд (в setInterval)
- Обработать ошибки загрузки

### Логика

```
onMount:
  ├─ Построить URL: http://{host}:{port}/api/stream.mjpeg?src={source}&t={Date.now()}
  ├─ Запустить setInterval каждые previewRefreshInterval мс
  │   └─ Обновить src с новым timestamp (для избежания кеша)
  └─ Вернуть cleanup функцию:
      └─ clearInterval

onImageLoad:
  └─ dispatch('ready')

onImageError:
  └─ dispatch('error', new Error(...))
```

### Требования

- Параметр `?t={Date.now()}` **обязателен** для избежания кеша браузера
- Функция `dispatchEvent()` для отправки событий в родитель
- Логи с префиксом `[CameraCardPreview]`

---

## 📋 CameraCardLive.svelte — WebRTC режим

### Самая сложная часть

WebRTC требует:
1. Создать RTCPeerConnection
2. Отправить WebSocket запрос на go2rtc, получить offer
3. Создать answer, отправить обратно
4. Получить MediaStream с медиа
5. При разрыве — переподключиться

### Логика инициализации

```
initWebRTC():
  TRY:
    ├─ Построить WebSocket URL: ws://{host}:{port}/api/webrtc?src={source}
    ├─ Создать RTCPeerConnection с STUN серверами
    ├─ Подписаться на ontrack
    │   └─ При получении трэка: mediaStream = stream, dispatch('ready')
    ├─ Подписаться на onconnectionstatechange
    │   └─ Если failed/disconnected → scheduleReconnect()
    ├─ Создать WebSocket
    ├─ На ws.onopen:
    │   ├─ Получить offer сообщение (с таймаутом webrtcTimeout)
    │   ├─ Вызвать setRemoteDescription(offer)
    │   ├─ Создать answer
    │   ├─ Вызвать setLocalDescription(answer)
    │   ├─ Отправить answer на WebSocket
    │   └─ ws.close() (обмен SDP завершён)
    │
    CATCH:
    ├─ Увеличить connectionAttempts++
    ├─ Если попыток < maxReconnectAttempts
    │   └─ scheduleReconnect() (попытка через interval)
    └─ Иначе dispatch('error', finalError)
```

### Функция scheduleReconnect

```
scheduleReconnect():
  ├─ Очистить текущие ресурсы (cleanup())
  ├─ setTimeout(initWebRTC, webrtcReconnectInterval)
  └─ Лог: [CameraCardLive] Scheduling reconnect in {interval}ms
```

### Функция cleanup

```
cleanup():
  ├─ Отменить reconnectTimeout (если есть)
  ├─ Закрыть все media tracks
  │   └─ mediaStream.getTracks().forEach(track => track.stop())
  ├─ Закрыть RTCPeerConnection
  │   └─ rtcConnection.close()
  └─ Сбросить переменные в null
```

### Требования

- Логирование КАЖДОГО шага: WebSocket connect, offer received, answer sent, track received
- Таймауты на все async операции (не должно зависать навечно)
- Максимум 5 попыток переподключения по умолчанию
- При каждой успешной подписке → логи `console.log('[CameraCardLive] ...')`

---

## 📋 СТИЛИ

Используй CSS переменные из дизайн-системы HA:
- `var(--color-surface)` — фон карточки
- `var(--color-primary)` — кнопка, активное состояние
- `var(--color-error)` — ошибки
- `var(--color-success)` — статус connected
- `var(--radius-lg)`, `var(--space-16)` и т.д.

Для спиннера:
```css
.spinner {
  width: 40px;
  height: 40px;
  border: 3px solid rgba(var(--color-primary-rgb), 0.2);
  border-top-color: var(--color-primary);
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}
```

---

## ✅ ОБЯЗАТЕЛЬНЫЕ ТЕСТЫ ПЕРЕД ОТПРАВКОЙ

```
[ ] Компилируется без ошибок (svelte-check, TypeScript)
[ ] Нет hydration warnings в консоли
[ ] Видео показывается (не чёрный экран)
[ ] При режиме 'live': WebSocket соединение открывается (DevTools Network)
[ ] При режиме 'preview': MJPEG картинка обновляется каждые N мс
[ ] При демонтировании: видны логи cleanup
[ ] При переподключении: счётчик connectionAttempts растёт, потом сбрасывается при успехе
[ ] Консоль чистая (нет warning'ов о missing deps, undefined, etc)
[ ] Go2rtc недоступен → показывается ошибка, появляется кнопка Retry
[ ] Нет утечек памяти (проверил в DevTools Memory при многократном монтировании)
```

---

## 📝 ИНСТРУКЦИИ КАК ДЕЛАТЬ

1. **Создай все 4 файла** в правильном порядке:
   - Сначала types (понять структуру)
   - Потом CameraCard (скелет)
   - Потом CameraCardPreview (простой MJPEG)
   - Потом CameraCardLive (сложный WebRTC)

2. **Следуй архитектуре ровно как описано** — не комбинируй компоненты, не переименовывай функции

3. **Используй примеры кода** — если там ровно такой же паттерн, не переписывай его

4. **Логируй всё** — каждая функция должна иметь `console.log` с префиксом компонента

5. **Обрабатывай ошибки** — fetch/WebSocket/setTimeout должны быть в try-catch

6. **Очищай ресурсы** — каждый onMount возвращает cleanup функцию

7. **Тестируй перед отправкой** — хотя бы видеоэлемент должен показывать картинку

---

## ⚠️ ЧЕГО НЕ ДЕЛАТЬ

- ❌ Не используй `$effect` для инициализации видео (используй `onMount`)
- ❌ Не переписывай архитектуру (4 компонента — обязательно)
- ❌ Не добавляй "улучшения" которых не в ТЗ
- ❌ Не скрывай ошибки (логируй и пробрасывай)
- ❌ Не забывай про очистку ресурсов
- ❌ Не используй localStorage (SecurityError в sandbox)
- ❌ Не пытайся "умно" оптимизировать (делай просто и понятно)

---

## 📚 СПРАВОЧНЫЕ МАТЕРИАЛЫ

Если что-то непонятно:

- [Svelte 5 Lifecycle](https://svelte.dev/docs/svelte/lifecycle-hooks) — onMount, onDestroy
- [GitHub Issue #3492](https://github.com/sveltejs/svelte/issues/3492) — srcObject problem & solution
- [SvelteKit SSR](https://joyofcode.xyz/learn-how-sveltekit-works) — как работает hydration
- [WebRTC API MDN](https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API) — RTCPeerConnection
- [go2rtc GitHub](https://github.com/AlexxIT/go2rtc) — документация, примеры URL

---

## 🎯 ФИНАЛЬНАЯ ПРОВЕРКА

Перед отправкой результата, убедись что:

1. ✅ Все 4 файла созданы и на месте
2. ✅ TypeScript типы правильные (компилируется)
3. ✅ CameraCard показывает видео без ошибок
4. ✅ WebRTC подключается или показывает ошибку с retry
5. ✅ MJPEG картинка обновляется
6. ✅ Нет утечек памяти
7. ✅ Консоль полна логами из компонентов
8. ✅ Ничего странного не вижу при открытии DevTools

Если хоть что-то из этого не пройдено — **не отправляй результат, отправь список проблем**.

---

## УСПЕХОВ! 🚀

Это ТЗ содержит всё что нужно. Следуй инструкциям ровно как есть, и получится профессиональный компонент.

Если что-то непонятно — спрашивай, я подробно объясню.